#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    for key, value in a_dictionary.items():
        if value == None:
            continue
        else:
            a_dictionary["key"] = lambda x: x * 2, value
        
